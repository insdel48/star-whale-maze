# 把《阿洛与星鲸》带到外面玩

## 为什么不能直接使用当前的 `file://` 地址？

浏览器为了安全，不允许本地文件注册离线缓存。直接打开 `index.html` 可以试玩，但不能可靠地添加为离线 App。正式安装需要一个以 `https://` 开头的网址。

## 第一步：发布到 HTTPS 静态网站

这是一个纯静态游戏，不需要服务器程序。把以下文件和文件夹原样上传到任意支持 HTTPS 的静态托管服务即可：

- `index.html`
- `app.js`
- `styles.css`
- `sw.js`
- `manifest.webmanifest`
- `icons/`
- `assets/`

发布完成后应得到类似 `https://example.com/maze/` 的地址。请确认这个地址可以正常打开游戏。

## 第二步：安装到 iPad

1. 使用 Safari 打开发布后的 HTTPS 地址。
2. 点击 Safari 工具栏里的“分享”图标。
3. 向下找到“添加到主屏幕”。
4. 名称保留为“星鲸迷宫”，点击“添加”。
5. 回到主屏幕，点击星鲸图标打开一次。

首次打开需要联网保存游戏文件。以后即使没有网络，也可以从主屏幕启动。

## Android 平板

使用 Chrome 或系统浏览器打开 HTTPS 地址，然后选择菜单里的“安装应用”或“添加到主屏幕”。

## 更新游戏

发布新版本后，在联网状态下打开游戏并重新进入一次。新文件会在后台替换旧的离线缓存。
